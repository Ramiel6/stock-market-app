import { ReplaceExtraPipe } from './replace-extra.pipe';

describe('ReplaceExtraPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceExtraPipe();
    expect(pipe).toBeTruthy();
  });
});
